spring.application.name=photo-to-pdf

# Server port
server.port=8080

# File upload size limits (increase if you upload large photos)
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=25MB
